var array=[1,2,3,4,5,6....]
for(var i=0,sum=0;i<array.length;sum+=array[i++]);
comsole.log(sum);


----------------------------------------------------------



function sumArray(array){
	for(
      var 
        index=0,
        length=array.length,
        sum=0;
      index<length;
      sum+=array[index++]
	);
   return sum;
}
